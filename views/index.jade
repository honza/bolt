h1 Bolt

p
    textarea(cols=50, rows=5, id='send-text')
p
    input(type='button', id='send-button', value='Send')


div#messages
    - messages.forEach(function(message){
        div.message
            - message = JSON.parse(message);
            strong= message.author
            span= message.body
            abbr(
                class='timeago'
                title=message.sent
            )
    - })

script
    var home = true;
