h3 Users

ul#users
    - each u in users
        li
            - if (u.follow)
                #{u.username} <span>following</span>
            - else
                <a href="/users/#{u.id}">#{u.username}</a>
                <span><a class="follow" href="#" rel="#{u.id}">follow</a></span>

